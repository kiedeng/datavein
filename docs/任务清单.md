# 项目整体任务清单

对应设计方案 V2.1 与 15 章排期。状态按仓库当前真实进度标注,每次迭代更新本文件。

图例:✅ 完成并验证 | 🔶 代码完成待真实环境验证 | ⬜ 未开始 | 🏢 只能在行内环境做

**总体进度:纯开发任务基本完成(全仓 83 个自动化测试全绿,其中 29 个跑在真实 MySQL 8 上);
剩余工作集中在 🏢 行内环境对接、制度推行与真实数据验收,以及少量 UI 增强。**

---

## M0 制度与存量收集(2周,与 M1 并行)🏢

准出:存量 SQL 入仓 ≥90%,target_table/owner/domain 登记完整率 ≥95%

- ⬜🏢 "不入仓不上线"写入发布流程,随立项颁布(**全项目最大风险项**)
- ⬜🏢 调度平台 API 打通(适配器已就绪,填 SCHEDULER_* + FIELD_MAP 配置)
- ⬜🏢 存量 SQL 批量导入与登记补全(动态拼接 SQL 的渲染归档改造)
- ⬜🏢 任务发布钩子对接 `pipeline.cli incremental`(钩子侧脚本)

## M1 血缘底座(4-5周)

准出:金标准抽检表级 ≥99% / 字段级 ≥95%;全量重建 <2h;覆盖率大盘可按 owner 下钻

**已完成(E2E 在真实 MySQL 8 验证):**
- ✅ 全量建库 DDL(6 个脚本)
- ✅ 解析核心:qualify + 逐列 lineage;CTE 穿透/自环标记/降级策略/位置列映射
  (Hive 语义)/Spark hint 过滤/multi-insert 专用原因码
- ✅ 语料库测试:13 段真实风格 Hive/Spark 信贷 ETL(5.3 疑难形态全覆盖)
- ✅ sqllineage 双引擎表级交叉校验(不一致进复核队列)
- ✅ 闭包:全量重算 + 2.2 增量修补(含退化阈值)
- ✅ **tmp 穿透折叠(5.3)**:表级/字段级合成 is_derived 边、多级链、表达式组合附注、
  confidence 取链上最低;全量影子表折叠 + 增量同任务折叠(跨任务由每日全量收敛)
- ✅ **非自环环路检测(5.4)**:闭包自联接检出,进 pipeline_run.stats + 告警日志
- ✅ 影子表原子切换 + 行数波动校验;并行解析 + 60s 超时;单事务换边
- ✅ 增量链路(hash 判变→单条解析→闭包区域修补)
- ✅ **血缘补录 API**(门户:failed 队列 + 人工画边,manual 边幂等替换)
- ✅ pipeline_run 运行记录 + healthcheck 告警命令
- ✅ CI:单测 + 真实 MySQL 集成测试

**待完成(全部 🏢):**
- ⬜🏢 Metastore 适配器连真实环境调通(直读后端库代码就绪)
- ⬜🏢 金标准抽检:100 段真实生产 SQL 人工标注比对(语料库格式即模板)
- ⬜🏢 ODS 采集映射血缘导入(src_type=ingest_map,采集平台配置对接)
- ⬜🏢 5 千段 SQL 全量重建压测(<1h)+ 枢纽表闭包退化专项压测

## M2 对话 PoC(1-2周)

准出:开发组演示通过,灰度启动;检索 Top1 ≥80%,families 零错并

**已完成:**
- ✅ FastMCP 8 工具全部真实现(血缘 5 + 取数 3)
- ✅ 检索三级:L1 精确 + L2 全文/rapidfuzz 模糊(阈值85)+ **L3 向量兜底**
  (Embedder 可插拔:HttpEmbedder 接 bge 服务化端点/HashEmbedder 测试用;
  Chroma optional import,不可用静默跳过)
- ✅ **向量构建流水线**(vectorize 命令):6.4 入库准入过滤(空/过短/无信息量),
  embedding 输入拼接,按注释 hash diff 增量
- ✅ 血缘族归并升级:并查集传递归族(消除顺序依赖)+ 6.3 四信号排序;
  tmp 表排除出检索候选
- ✅ **get_lineage_path 明细路径**(闭包判连通 + BFS 最短路逐跳)
- ✅ **消歧评估集:50 组问法,Top1 实测 100%(门槛 80%),同链/异链专项全过**
- ✅ Redis 热点缓存;Claude Code 三件套
- ✅ Agent 系统提示词(9.1 全要素,portal/llm.py 与 CLAUDE.md 同源要点)

**待完成:**
- ⬜🏢 DeepSeek 端点接入(new-api;LLM 客户端就绪,填 LLM_BASE_URL 即可)
- ⬜🏢 bge-base-zh 服务化部署 + 125 万字段注释首次向量化(离线数小时)
- ⬜🏢 MCP token 按人发放映射 user_ctx(nginx 单 token 已就绪)
- ⬜🏢 开发组灰度 2 周 + bad case 修复

## M3 团队试用(2周)

准出:数分组可用,消歧指标达标

- ✅ 简版门户:对话页(SSE 流式 + function-calling 工具循环 ≤5 轮)
- ✅ 降级模式(9.3):LLM 未配置/异常自动转结构化卡片,血缘查询不因模型故障不可用
- ✅ 覆盖率大盘页(分层覆盖率/层×域×owner 下钻/failed 原因码/medium 边占比)
- ✅ 术语管理页(CRUD/certified 认领/status 流转)
- ✅ 血缘补录页(failed 队列 + 人工画边)
- ✅ 反馈闭环(好评差评 + 评论落 feedback 表)
- ✅ 门户全链路审计(channel=portal 含 tool_trace)
- ⬜🏢 术语首批 50 条(各域 owner 供稿)
- ⬜🏢 SSO/LDAP 登录集成(X-User 头占位,代码留对接点)

## M4 可信取数(4-5周)

准出:取数用例 100 例全等,权限用例全部拦截

**已完成:**
- ✅ 指标 YAML Schema(附录 B 落地)+ 加载校验 + 版本管理,3 个示例指标
- ✅ plan→SQL 确定性编译器(7.2 四保障:固定排序/版本锁死/golden 入 CI/无随机源)
- ✅ 查询执行网关:只读白名单/敏感表黑名单/强制 LIMIT/超时/并发上限
- ✅ 权限:L4 拒出/L3 白名单+掩码/行权限变量注入(缺失即拒,宁严勿松)——
  权限用例 8 例全部拦截验证
- ✅ 全量审计(拒绝留痕/row_policy_applied/指标版本/按客户查询标记)
- ✅ 受控 Text-to-SQL 兜底(LLM 可插拔/表集闭包校验/EXPLAIN 预检/强制标注)
- ✅ MCP 三工具接通网关(query_metric/run_adhoc_sql 含检索圈表/list_metrics)
- ✅ 取数评估集:golden 用例 + 权限拦截用例入 CI

**待完成:**
- ⬜🏢 首批 20 指标建模(业务口径确认;YAML 模板与 golden 流程已就绪)
- ⬜🏢 行权限数据同步(行内权限系统→user_ctx,每小时;注入机制已验证)
- ⬜🏢 数仓只读账号申请与 WAREHOUSE_* 配置(Hive/Spark 方言时网关适配复核)
- ⬜🏢 数据安全评审 + 取数单独灰度(白名单+非敏感域先行)
- ⬜🏢 取数 100 例真实口径比对(20 指标 × 5 问法,vs 人工 SQL 全等)

## M5 正式门户(3-4周)

准出:验收会通过,全员开放

- ✅ 对话主界面(SSE)+ SQL 收据卡片样式(含"自由查询"警示视觉区分)
- ✅ 降级模式(9.3)
- 🔶 血缘可视化:当前为内联 SVG 分层图(内网零外部依赖);G6 交互卡片
  (点击下钻/300 节点上限/域折叠)为后续增强,需前端支援
- ⬜ 管理页:审计查询/指标模型浏览(数据接口已有,页面未做)
- ⬜ 门户接入 M4 取数(/api/metric/query 已留占位,接 GATEWAY_URL 即通)
- ⬜🏢 前端支援到位(G6/视觉打磨)

## 横向工程任务(贯穿)

- ✅ CI(单测+golden+权限用例+两套 MySQL E2E);影子表可回滚;运维手册
- ✅ 生产编排(nginx TLS/token/mcp 双副本/gateway/portal/健康检查/每日备份)
- ✅ 评估集持续回归:全部固化为 pytest(语料库/消歧/golden/权限),
  提示词、指标 YAML、sqlglot 版本变更跑 CI 即回归
- ✅ 运营周报自动化(weekly-report 命令)
- ⬜🏢 恢复演练首次执行(此后每季度)
- ⬜🏢 渗透测试(行内安全团队按规执行)

---

## 上线前行内清单(按依赖序)

1. **M0 制度颁布 + 存量 SQL 入仓**(最长路径,先行)
2. 填 `.env`:Metastore 后端库只读账号 / 调度平台端点与字段映射 / 数仓只读账号 /
   new-api LLM 端点 / bge embedding 端点 / 行内 CA 证书
3. `docker compose -f deploy/docker-compose.prod.yml up -d` + `make init-db`
4. 首轮数据装载与金标准抽检(100 段真实 SQL,13 章指标)
5. bge 部署 + `pipeline.cli vectorize` 首次向量化
6. 术语首批 50 条 + 开发组灰度(M2)→ 数分组(M3)
7. 20 指标业务建模 + 行权限同步 + 安全评审 → 取数灰度(M4)
