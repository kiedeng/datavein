# 项目整体任务清单

对应设计方案 V2.1 与 15 章排期。状态按仓库当前真实进度标注,每次迭代更新本文件。

图例:✅ 完成并验证 | 🔶 代码完成待真实环境验证 | ⬜ 未开始 | 🏢 只能在行内环境做

**总体进度:约 35%(M1 代码层全链路已在真实 MySQL 验证;M2 服务层大半;M3-M5 未开始)**

---

## M0 制度与存量收集(2周,与 M1 并行)🏢

准出:存量 SQL 入仓 ≥90%,target_table/owner/domain 登记完整率 ≥95%

- ⬜🏢 "不入仓不上线"写入发布流程,随立项颁布(**全项目最大风险项**)
- ⬜🏢 调度平台 API 打通(`.env` 配 SCHEDULER_* + FIELD_MAP,适配器已就绪)
- ⬜🏢 存量 SQL 批量导入与登记补全(动态拼接 SQL 的渲染归档改造)
- ⬜🏢 任务发布钩子对接 `pipeline.cli incremental`(钩子侧脚本)

## M1 血缘底座(4-5周)

准出:金标准抽检表级 ≥99% / 字段级 ≥95%;全量重建 <2h;覆盖率大盘可按 owner 下钻

**已完成(本仓库,E2E 已在真实 MySQL 8 验证):**
- ✅ 全量建库 DDL(6 个脚本,建库即用)
- ✅ 解析核心:qualify + 逐列 lineage;CTE 穿透/自环标记/降级策略/位置列映射
  (Hive 语义)/Spark hint 过滤/multi-insert 专用原因码
- ✅ 语料库测试:13 段真实风格 Hive/Spark 信贷 ETL(窗口去重/UNION 自依赖/
  LATERAL VIEW/GROUPING SETS/DISTRIBUTE BY/视图/select */tmp 链/多任务写同表)
- ✅ sqllineage 双引擎表级交叉校验(不一致标记进复核队列)
- ✅ 闭包:全量重算 + 2.2 增量修补(含退化阈值);菱形/环路单测
- ✅ 影子表原子切换 + 行数波动校验(边表与闭包表,校验不过保旧版)
- ✅ 并行解析(8 进程)+ 单 SQL 60s 超时降级
- ✅ 按 sql_id 单事务换边;多任务写同表边共存(E2E 验证)
- ✅ 增量链路:hash 判变 → 单条解析 → 闭包区域修补(E2E 验证)
- ✅ pipeline_run 运行记录 + healthcheck 告警命令
- ✅ CI:单测 + 真实 MySQL 集成测试

**待完成:**
- ⬜🏢 Metastore 适配器连真实环境调通(直读后端库代码已就绪,须验证行内
  Metastore 版本的表结构差异)
- ⬜🏢 金标准抽检:100 段真实生产 SQL 人工标注比对(语料库格式即模板)
- ⬜🏢 ODS 采集映射血缘导入(src_type=ingest_map,采集平台配置对接)
- ⬜ 临时表穿透折叠(5.3:A→tmp→B 合成 is_derived 边,tmp 不建正式节点)——
  解析已识别 tmp 层,折叠后处理未实现
- ⬜ 环路检测告警(5.4:非自环环路推送双方 owner)
- ⬜ 覆盖率大盘前端页(数据接口已有,页面归 M3)
- ⬜🏢 5 千段 SQL 全量重建压测(<1h 目标)+ 枢纽表闭包退化专项压测
- ⬜ 血缘补录 API(src_type=manual 写入 + failed 队列查询接口)

## M2 对话 PoC(1-2周)

准出:开发组演示通过,灰度启动;检索 Top1 ≥80%,families 零错并

**已完成:**
- ✅ FastMCP 7 工具框架 + 5 个血缘工具真实现(E2E 验证核心查询)
- ✅ search_term L1 精确 + L2 全文;血缘族归并(闭包判同链/异链)
- ✅ get_lineage BFS(visited 防环/200 节点截断/表达式内嵌)
- ✅ impact_analysis(闭包一跳/按任务聚合/分页)
- ✅ Redis 热点缓存(TTL 到次日重建)
- ✅ Claude Code 三件套(.mcp.json/CLAUDE.md/data-lineage Skill)

**待完成:**
- ⬜ L2 rapidfuzz 术语模糊匹配接入(依赖已声明,逻辑未接)
- ⬜ L3 向量检索:bge-base-zh + Chroma 双 collection;字段注释离线批量向量化
  (125 万条首次数小时)+ 按元数据 diff 增量
- ⬜ 注释入库准入过滤(6.4:空/过短/无信息量不进向量库)
- ⬜ get_lineage_path 明细路径(连通性已通,逐跳明细未实现)
- ⬜ MCP 服务鉴权细化(当前 nginx Bearer 单 token;按人发 token 映射 user_ctx)
- ⬜🏢 DeepSeek 端点接入(new-api)+ Agent 系统提示词全文(9.1 要点已定)
- ⬜ 消歧评估集:50 组问法(含别名/口语/歧义词)自动化断言
- ⬜🏢 开发组灰度 2 周 + bad case 修复

## M3 团队试用(2周)

准出:数分组可用,消歧指标达标

- ⬜ OpenWebUI 或简版门户接入(SSE 流式对话)
- ⬜ 术语字典首批 50 条(各域 owner 供稿,🏢)+ 术语管理页(增改/审批/certified)
- ⬜ 覆盖率大盘页(层×域×owner 下钻,failed/degraded/注释缺失到人)
- ⬜ 血缘补录页(failed 队列人工画边)
- ⬜ 反馈按钮 + feedback 落库闭环

## M4 可信取数(4-5周)

准出:取数用例 100 例全等,权限用例全部拦截

- ⬜ 指标 YAML Schema 定义 + Git 仓库与评审流程
- ⬜ 首批 20 指标建模(业务口径确认,🏢)
- ⬜ plan→SQL 编译器(7.2 确定性四保障:固定排序/版本锁死/golden 用例/无随机源)
- ⬜ 查询执行网关:只读账号池/LIMIT/超时/并发限流
- ⬜🏢 行权限注入(row_policy 变量 + 行内权限系统每小时同步)
- ⬜ sens_level 结果脱敏(L3 掩码/L4 拒出)+ 敏感表黑名单硬编码
- ⬜ query_audit 全链路落库(row_policy_applied/指标版本/is_customer_query)
- ⬜ 兜底 Text-to-SQL 通道(检索圈表/静态校验/EXPLAIN 预检/强制标注)
- ⬜ query_metric / run_adhoc_sql 两个 MCP 工具接网关
- ⬜ 取数评估集:核心 20 指标 × 5 问法 + 越权/敏感/行权限拦截用例
- ⬜🏢 数据安全评审 + 取数单独灰度(白名单+非敏感域先行)

## M5 正式门户(3-4周)

准出:验收会通过,全员开放

- ⬜ 对话主界面(SSE)+ 会话管理
- ⬜ 血缘 G6 卡片(表级聚合默认/点击下钻/300 节点上限/超限转清单)
- ⬜ SQL 收据卡片(数据+SQL+口径+数据时间,语义层/自由查询视觉区分)
- ⬜ 管理页:审计查询/指标模型浏览
- ⬜ 降级模式:模型故障时搜索框直连 REST 结构化卡片(9.3)
- ⬜🏢 前端支援到位(人力假设中的 M5 依赖)

## 横向工程任务(贯穿)

- ✅ CI(单测+集成+语法);影子表可回滚;运维手册(部署/监控/故障处置/备份)
- ✅ 生产编排(nginx TLS/token/双副本/健康检查/每日备份)
- ⬜ Agent 回答质量评估集持续回归框架(13 章:提示词/模型/指标变更门禁)——
  消歧与取数评估集分属 M2/M4,此处指统一 runner
- ⬜🏢 恢复演练首次执行(此后每季度)
- ⬜🏢 渗透测试(行内安全团队按规执行)
- ⬜ 运营周报自动化(覆盖率/Top1 命中/采纳率/拒答分布,数据源均已有)

---

## 关键依赖链(排期用)

```
M0 存量SQL入仓 ──► M1 金标准抽检 ──► M2 灰度 ──► M3 试用
                                   │
Metastore调通 ──► 全量重建压测 ──────┘
                                        M4 指标建模(业务确认)──► 取数灰度 ──► M5 全员
```

最长路径在 M0/M1 的行内对接与制度推行;纯开发任务(M4 编译器/M5 前端)可与其并行预研。
